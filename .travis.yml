# os: linux

# group: edge

# script:
#   # Check .cargo folder permission. Should be owned by travis user
#   - ls -la $HOME 
#   - ls -la $HOME/.cargo
#   - cosign -version

# language: python
# python:
#   - "3.8"

# jobs:
#   include:
#     - arch: ppc64le
#       dist: focal
#     - arch: ppc64le
#       dist: bionic
#     - arch: s390x
#       dist: bionic
#     - arch: s390x
#       dist: focal

dist: bionic
group: edge
script:
- echo -e "-Xms2048M\n-Xmx2048M\n-Xss6M" > /tmp/jvmopts
- sudo mkdir -p /etc/sbt/
- sudo mv /tmp/jvmopts /etc/sbt/jvmopts
- sudo cat /etc/sbt/jvmopts
arch:
  - amd64
  - ppc64le
  - arm64
  - s390x
os: linux
