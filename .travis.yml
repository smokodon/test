jobs:
  include:
   - dist: bionic
     group: dev
     services:
      - docker
     script:
      - npm list -g
      - nvm ls
      - nvm install node
      - nvm ls
      - find / | grep bundler
      - find / | grep jackson