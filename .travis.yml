jobs:
  include:
   - dist: bionic
     group: dev
     services:
      - docker
     script:
      - find / | grep oauth
      - find / | grep kotlin
      - find / | grep keypair
      - find / | grep minimist
      - find / | grep shell-quote
      - find / | grep rexml
      - find / | grep mongo-driver
  
