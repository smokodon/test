jobs:
  include:
    - os: linux
      dist: xenial
      language: shell
      script:
        - python --version
        - eval "$(pyenv init --path)"
        #- pyenv install 3.7.1
        - pyenv global 3.7.1
        - python -V
