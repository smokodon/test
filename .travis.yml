jobs:
  include:
   - dist: xenial
     arch: amd64
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: bionic
     arch: amd64
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: bionic
     arch: arm64
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: bionic
     arch: s390x
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: bionic
     arch: ppc64le
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: focal
     arch: amd64
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: focal
     arch: arm64
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: focal
     arch: s390x
     script:
      - ./script
      - cosign
      - rekor-cli
   - dist: focal
     arch: ppc64le
     script:
      - ./script
      - cosign
      - rekor-cli

