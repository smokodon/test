jobs:
  include:
  - dist: jammy
    script:
    - apt list -a
    - docker-compose --version
    - docker --version
    - groovy -version
    - mongodb --version
    - mongod --version
    - redis-server --version