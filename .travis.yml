jobs:
  include:
   - dist: bionic
     arch: s390x
     #jdk: openjdk13
     script:
      - sudo add-apt-repository universe
      - sudo apt-get update
      - sudo apt install openjdk-13-jdk
      - java -version
      - echo $JAVA_HOME
      - echo $PATH