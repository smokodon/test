group: edge

script:
  - openssl version
  - wget https://www.openssl.org/source/openssl-3.0.7.tar.gz
  - tar –xvzf openssl-3.0.7.tar.gz –C /openssl
  - cd openssl/openssl-3.0.7
  - sudo make install


jobs:
  include:
    - arch: amd
      dist: jammy
