jobs:
  - os: linux
    dist: xenial
    group: dev
    script:
      - pyenv versions
      - pip install -U pip
  - os: linux
    python: 3.7
    dist: xenial
    env:
      - PYTHONPATH=.
    script:
      - python -V
      - python -m pip --version
      - pyenv global 3.7.1
      - python -V
      - pip install --user pip
      - python -m pip --version
      - pyenv versions
      - pip install -U pip