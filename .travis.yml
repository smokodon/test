jobs:
  include:
   - dist: xenial
     jdk: jdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     jdk: oraclejdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: jdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH