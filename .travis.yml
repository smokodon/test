jobs:
  include:
   - dist: bionic
     group: dev
     lang: python
     python: 2.7.18
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version
   - dist: focal
     group: dev
     lang: python
     python: 3.9.13
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version


