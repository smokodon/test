jobs:
  include:
   - dist: bionic
     group: dev
     language: python
     python: 2.7.17
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version
   - dist: focal
     group: dev
     language: python
     python: 3.8.3
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version


