language: java
services:
  - mongodb
dist: focal
group: edge
script:
  - sudo systemctl start mongodb
  - sudo systemctl start mongodb
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'