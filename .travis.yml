jobs:
  include:
   - dist: focal
     group: dev
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: dev
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: ppc64le
     group: dev
     group: dev
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: dev
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: dev
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: s390x
     group: dev
     group: dev
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: dev
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: dev
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: arm64
     group: dev
     group: dev
     script:
      - pip install cargo
      - ant -version

  #  - dist: focal
  #    arch: ppc64le
  #    group: dev
  #    language: java
  group: dev#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH
  #  - dist: bionic
  #    arch: ppc64le
  #    group: dev
  #    language: java
  group: dev#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH
  #  - dist: xenial
  #    arch: ppc64le
  #    group: dev
  #    language: java
  group: dev#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH


