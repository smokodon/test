jobs:
  include:
  - dist: bionic
    group: dev
    script:
     - which -a ruby
     - python --version
     - easy_install --version
     - py.test --version
  - dist: focal
    group: dev
    script:
     - which -a ruby
     - python --version
     - easy_install --version
     - py.test --version

