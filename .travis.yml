jobs:
  include:
   - dist: focal
     jdk: jdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: oraclejdk13
     arch: arm64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: jdk13
     arch: ppc64le

