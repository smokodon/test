jobs:
  include:
   - dist: rhel8
     arch: arm64
     services:
     - docker
     language: node_js
     group: dev
     node_js:
     - 8
     script:
     - cat /etc/redhat-release
   - dist: rhel8
     arch: arm64
     script:
     - cat /etc/redhat-release
   - dist: rhel8
     arch: arm64
     language: node_js
     node_js:
     - 8
     script:
     - cat /etc/redhat-release


