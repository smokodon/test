jobs:
  include:
   - dist: focal
     group: edge
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: ppc64le
     group: edge
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: edge
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: s390x
     group: edge
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: edge
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: arm64
     group: edge
     script:
      - pip install cargo
      - ant -version


