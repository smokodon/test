jobs:
  include:
  - dist: bionic
    group: dev
    script:
     - easy_install --version
     - py.test --version
  - dist: focal
    group: dev
    script:
     - easy_install --version
     - py.test --version
