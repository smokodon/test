

script:
   - sudo ls -al /etc/sbt/ || true
   - sudo cat /etc/sbt/jvmopts || true

jobs:
  include:
    - arch: ppc64le
      dist: bionic
    - arch: ppc64le
      dist: focal
    - arch: s390x
      dist: bionic
    - arch: s390x
      dist: focal
    - arch: amd64
      dist: focal