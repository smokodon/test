jobs:
  include:
  - dist: bionic
    group: dev
    script:
     - ls -ls /usr/bin/python*
     - ls -ls /usr/local/bin/python*
     - which -a ruby
     - python --version
     - easy_install --version
     - py.test --version
  - dist: focal
    group: dev
    script:
     - ls -ls /usr/bin/python*
     - ls -ls /usr/local/bin/python*
     - which -a ruby
     - python --version
     - easy_install --version
     - py.test --version
