jobs:
  include:
   - dist: xenial
     arch: s390x
     #jdk: openjdk13
     script:
      - sudo apt-get update
      - sudo apt-get install openjdk-11-jdk
      - java -version
      - echo $JAVA_HOME
      - echo $PATH