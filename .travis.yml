os: linux
dist: xenial
arch:
  #- s390x
  #- ppc64le
  - arm64
  #- amd64
script:
  - docker help
  - docker run  -t ubuntu:latest bash -c 'nproc'
  - nproc
  - docker -t -v /proc/cpuinfo:/proc/cpuinfo:rw -v /proc/meminfo:/proc/meminfo:rw -v /proc/stat:/proc/stat:rw node:latest bash
  - docker run --rm -t node:latest node -e "console.log("CpusLength: ", getCpusLength());"
  - echo "I scream and I SHOUT" 1>&2
  - ls -l /proc/$$/fd
  - ls -l /proc/$$/fd/1
  - ls -l /proc/$$/fd/2