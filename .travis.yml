script:
- sudo apt-get update
- sudo apt-get upgrade -y
- git --version

group: dev

jobs:
  include:
    - os: linux
      dist: xenial
    - os: linux
      dist: bionic
    - os: linux
      dist: focal
