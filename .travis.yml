group: edge

script:
  - openssl version
  - wget -q https://www.openssl.org/source/openssl-3.0.7.tar.gz
  - tar –xvzf openssl-3.0.7.tar.gz
  - ./Configure
  - make test
  - sudo make install


jobs:
  include:
    - arch: amd
      dist: jammy
