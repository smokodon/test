jobs:
  include:
   - dist: focal
     language: python
     group: edge
     arch: ppc64le
     script:
      - python -m pip install cargo
      - ant -version
   - dist: bionic
     language: python
     group: edge
     arch: ppc64le
     script:
      - python -m pip install cargo
      - ant -version
   - dist: xenial
     language: python
     arch: ppc64le
     group: edge
     script:
      - python -m pip install cargo
      - ant -version
   - dist: focal
     language: python
     group: edge
     arch: s390x
     script:
      - python -m pip install cargo
      - ant -version
   - dist: bionic
     language: python
     group: edge
     arch: s390x
     script:
      - python -m pip install cargo
      - ant -version
   - dist: xenial
     language: python
     arch: s390x
     group: edge
     script:
      - python -m pip install cargo
      - ant -version



