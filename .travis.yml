jobs:
  include:
   - dist: xenial
     jdk: openjdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     jdk: oraclejdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: openjdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
