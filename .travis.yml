jobs:
  include:
   - dist: bionic
     group: dev
     services:
      - docker
     script:
      - node --version
      - heroku version
      - find / | grep keypair
      - find / | grep minimist
      - find / | grep shell-quote
      - find / | grep rexml
      - find / | grep mongo-driver
  
