script:
- wget --no-verbose  "https://dl.google.com/dl/linux/direct/google-chrome-stable_current_amd64.deb" && sudo apt -qq update && sudo apt-get install ./google-chrome-stable_current_amd64.deb
# - git --version
# - sudo add-apt-repository ppa:git-core/ppa -y
# - sudo apt-get update
# - sudo apt install git
# - git --version

group: stable

jobs:
  include:

    - dist: xenial
      arch: amd
    # - dist: bionic
    #   arch: ppc64le
    # - dist: focal
    #   arch: ppc64le
    # - dist: xenial
    #   arch: s390x
    # - dist: bionic
    #   arch: s390x
    # - dist: focal
    #   arch: s390x
    # - dist: xenial
    #   arch: arm64
    # - dist: bionic
    #   arch: arm64
    # - dist: focal
    #   arch: arm64
