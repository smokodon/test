jobs:
  include:
   - dist: rhel8
     script:
     - cat /etc/redhat-release
   - dist: rhel8
     script:
     - cat /etc/redhat-release
   - dist: rhel8
     script:
     - cat /etc/redhat-release


