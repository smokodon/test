jobs:
  include:
   - dist: focal
     language: generic
     script:
     - p4 --help
   - dist: bionic
     language: generic
     script:
     - p4 --help
   - dist: focal
     group: dev
     language: generic
     script:
     - p4 --help
   - dist: bionic
     group: dev
     language: generic
     script:
     - p4 --help
   - dist: focal
     script:
     - p4 --help
   - dist: bionic
     script:
     - p4 --help
    
