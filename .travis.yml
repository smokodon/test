jobs:
  include:
   - dist: bionic
     group: dev
     python: 3.7.13
     script:
     - easy_install --version
     - py.test --version
   - dist: focal
     group: dev
     python: 3.7.13
     script:
     - easy_install --version
     - py.test --version
