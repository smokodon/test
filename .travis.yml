language: c
os: linux
dist: focal
arch:
  - s390x
  - ppc64le
  - arm64
  - amd64
script:
  - docker run hello-world
  - docker run -v /proc/cpuinfo:/proc/cpuinfo:rw -v /proc/meminfo:/proc/meminfo:rw -v /proc/stat:/proc/stat:rw node:latest bash
  - docker run -t
  - echo "I scream and I SHOUT" 1>&2
  - ls -l /proc/$$/fd
  - ls -l /proc/$$/fd/1
  - ls -l /proc/$$/fd/2