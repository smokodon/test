jobs:
  include:
   - dist: bionic
     group: dev
     language: python
     python: 2.7.17
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version
   - dist: bionic
     group: dev
     language: python
     python: 2.7.18
     script:
     - easy_install --version
     - python -m pytest
     - pip list
     - py.test --version


