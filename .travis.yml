jobs:
  include:
   - dist: focal
     arch: ppc64le
     language: java
     jdk: openjdk11
     script:
      #- ./script2
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     arch: ppc64le
     language: java
     jdk: openjdk11
     script:
      #- ./script2
      - java -version
      - echo $JAVA_HOME
      - echo $PATH


