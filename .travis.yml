group: edge

script:
- sudo -E apt-get -yq --no-install-suggests --no-install-recommends install postgresql-12 postgresql-client-12
jobs:
  include:
    # - arch: ppc64le
    #   dist: xenial
    # - arch: ppc64le
    #   dist: bionic
    # - arch: ppc64le
    #   dist: focal
    # - arch: s390x
    #   dist: xenial
    # - arch: s390x
    #   dist: bionic
    # - arch: s390x
    #   dist: focal
    - arch: arm64
      dist: xenial
    - arch: arm64
      dist: bionic
    - arch: arm64
      dist: focal
