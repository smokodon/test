script:
- git --version
- add-apt-repository ppa:git-core/ppa
- sudo apt-get update
- sudo apt install git
- git --version

group: dev

jobs:
  include:
    - os: linux
      dist: xenial
    - os: linux
      dist: bionic
    - os: linux
      dist: focal
    - os: linux
      dist: jammy