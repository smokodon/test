jobs:
  include:
   - dist: trusty
     arch: amd64
     language: android
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: amd64
     language: java
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: android
     jdk: openjdk15
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: xenial
     arch: s390x
     language: java
     jdk: openjdk
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: xenial
     arch: amd64
     language: android
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     arch: amd64
     language: java
     jdk: openjdk15
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     arch: amd
     language: java
     jdk: openjdk12
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
