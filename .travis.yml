jobs:
  include:
   - dist: trusty
     arch: arm64
     language: android
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: java
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: android
     jdk: openjdk9
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: java
     jdk: openjdk9
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: android
     jdk: openjdk8
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: trusty
     arch: arm64
     language: java
     jdk: openjdk8
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: xenial
     arch: arm64
     language: java
     jdk: openjdk11
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     arch: arm64
     language: java
     jdk: openjdk9
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     arch: arm64
     language: java
     jdk: openjdk12
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH

