jobs:
  include:
   - dist: focal
     group: edge
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: ppc64le
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: ppc64le
     group: edge
     group: dev
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: edge
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: s390x
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: s390x
     group: edge
     group: dev
     script:
      - pip install cargo
      - ant -version
   - dist: focal
     group: edge
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: bionic
     group: edge
     arch: arm64
     script:
      - pip install cargo
      - ant -version
   - dist: xenial
     arch: arm64
     group: edge
     group: dev
     script:
      - pip install cargo
      - ant -version

  #  - dist: focal
  #    arch: ppc64le
  #    group: edge
  #    language: java
  group: edge#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH
  #  - dist: bionic
  #    arch: ppc64le
  #    group: dev
  #    language: java
  group: edge#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH
  #  - dist: xenial
  #    arch: ppc64le
  #    group: dev
  #    language: java
  group: edge#    
  jdk: openjdk11
  #    script:
  #     #- ./script2
  #     - java -version
  #     - echo $JAVA_HOME
  #     - echo $PATH


