jobs:
  include:
   - dist: bionic
     group: dev
     services:
      - docker
     script:
      - find / | oauth
      - find / | grep kotlin
      - find / | keypair
      - find / | minimist
      - find / | shell-quote
      - find / | rexml
  
