jobs:
  include:
   - dist: bionic
     language: python
     python:
     - "3.7.13"
     group: stable
     script:
     - python -c "__import__('setuptools').setup()" sdist
     - pip install pip setuptools --upgrade
     - python -c "__import__('setuptools').setup()" sdist
   - dist: focal
     group: stable
     language: python
     python:
     - "3.7.13"
     #- "3.8"
     script:
     - python -c "__import__('setuptools').setup()" sdist
     - pip install pip setuptools --upgrade
     - python -c "__import__('setuptools').setup()" sdist
