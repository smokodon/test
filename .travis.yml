jobs:
  include:
   - dist: xenial
     jdk: openjdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: xenial
     jdk: openjdk13
     arch: arm64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: xenial
     jdk: openjdk13
     arch: ppc64le
   - dist: bionic
     jdk: openjdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     jdk: openjdk13
     arch: arm64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: bionic
     jdk: openjdk13
     arch: ppc64le
   - dist: focal
     jdk: openjdk13
     arch: amd64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: openjdk13
     arch: arm64
     script:
      - java -version
      - echo $JAVA_HOME
      - echo $PATH
   - dist: focal
     jdk: openjdk13
     arch: ppc64le

notifications:
  email: false
