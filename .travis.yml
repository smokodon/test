jobs:
  include:
  - dist: bionic
    group: dev
    script:
     - python --version
     - easy_install --version
     - py.test --version
  - dist: focal
    group: dev
    script:
     - python --version
     - easy_install --version
     - py.test --version

